# Legacy document-management prototype

This directory contains the original browser-side document management prototype.
It is retained for reference and targeted compatibility tests, but it is not loaded
by the extension and is not copied into the production build.

Active document upload and storage code lives under `utils/` and is used by the
settings and side-panel pages.
