# 「霓虹航海日志」测试风格指南

> 用途：验证文档生成器是否正确读取并遵循新的风格指南。
> 本指南刻意采用辨识度极高的表达和结构，不建议直接用于正式生产文档。

## 1. 核心气质

- 将整篇文档写成一份“数字航海日志”，但必须保持信息准确、步骤清晰、可以实际执行。
- 语气冷静、敏捷，带有轻微的未来感；不要写成小说，不要堆砌比喻。
- 使用简体中文。
- 句子尽量控制在 28 个汉字以内；一个句子只表达一个动作或结果。
- 禁止使用“首先、其次、然后、最后”等普通连接词。
- 用“启航、定位、校准、确认、归港”等词替代平淡的流程描述，但不得改变产品事实。

## 2. 强制风格指纹

生成的文档必须同时出现以下五种特征，否则视为没有遵循本指南：

1. 一级标题必须以 `✦` 开头。
2. 开场第一句话必须是：`信号已接入，开始记录本次航线。`
3. 每个二级标题必须使用 `航标 N｜标题` 格式，N 从 1 开始递增。
4. 每个操作步骤必须包含“动作”“回声”“坐标”三个字段。
5. 文档最后一行必须是：`—— 航线确认：记录完毕 ✦`

## 3. 标题体系

- 一级标题：`# ✦ 文档主题`
- 二级标题：`## 航标 1｜章节名称`
- 三级标题：`### 扫描点 A｜小节名称`
- 标题应短促，尽量不超过 16 个汉字。
- 不得使用“概述”“正文”“其他”等含义模糊的标题。

## 4. 步骤结构

每个步骤使用以下固定结构：

### 步骤 N｜动宾短语

- **动作：** 明确写出用户需要执行的操作。
- **回声：** 描述页面、系统或控件应出现的反馈。
- **坐标：** 写明按钮、菜单、输入框或页面区域的位置。

紧接着保留对应的截图占位：

`[截图N]`

规则：

- “动作”必须以动词开头。
- “回声”不得猜测未记录的系统反馈；无法确认时写“以实际页面反馈为准”。
- “坐标”应优先使用界面上可见的准确名称。
- 不得合并或遗漏录制步骤。

## 5. 信息模块

根据内容需要使用以下模块：

- 提示使用：`> ◇ 导航提示：内容`
- 警告使用：`> ⚠ 暗礁警告：内容`
- 成功状态使用：`> ✓ 信号确认：内容`
- 补充说明使用：`> ≋ 航线旁注：内容`

每种模块在同一章节中最多出现一次，避免视觉噪声。

## 6. 表格规则

当存在三个及以上并列项目时，优先使用表格。表头采用以下命名风格：

| 信标 | 含义 | 判定信号 |
|---|---|---|
| 项目名称 | 简明说明 | 可观察到的结果 |

- 表格单元格避免长段落。
- 判定条件必须明确、可观察。
- 不得为了满足风格而虚构数据。

## 7. 措辞规则

优先使用：

- “选择”而不是“点一下”
- “输入”而不是“填进去”
- “页面显示”而不是“可以看到”
- “确认”而不是“确保一下”
- “返回”而不是“回去”

禁止使用：

- 空泛赞美，例如“强大”“惊艳”“非常方便”
- 不确定承诺，例如“绝对成功”“一定不会出错”
- 连续三个以上感叹号
- 大段背景铺垫
- 与本次录制无关的账号、链接、数据或功能

## 8. 输出格式适配

- 输出为 Markdown 时，严格使用本指南给出的 Markdown 层级和模块语法。
- 输出为 HTML 时，将标题、列表、表格和提示模块转换为语义化 HTML；不得输出脚本或外部资源。
- 输出为纯文本时，保留“✦”“航标”“动作／回声／坐标”“暗礁警告”等风格指纹，但移除 Markdown 标记。
- 无论采用何种格式，都必须保留截图占位及其原始顺序。

## 9. 事实优先级

- 本次录制内容永远高于风格表达。
- 风格指南只控制结构、语气和版式，不得修改操作事实。
- 当风格规则与输出格式要求冲突时，以输出格式要求为准。
- 当缺少信息时，明确标注“不在本次记录范围内”，不得自行补全。

## 10. 固定收尾

正文结束后，用一句话总结用户已经完成的目标，不增加新的操作步骤。

文档最后一行必须原样输出：

`—— 航线确认：记录完毕 ✦`
